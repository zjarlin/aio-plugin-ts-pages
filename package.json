{
  "name": "aio-plugin-ts-pages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build:js": "tsc -p tsconfig.json",
    "build": "pnpm run build:js && node build/generator/main.js",
    "test": "pnpm run build:js && node --test test/**/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
